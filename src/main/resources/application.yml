spring:
  application:
    name: elt
  datasource:
    url: jdbc:mysql://localhost:3306/lottery_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

etl:
  schedule: false
  configId: 112